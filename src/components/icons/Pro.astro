---
const {
  size = 30,
  color = "currentColor",
  class: className = "",
} = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 256 256"
  width={size}
  height={size}
  fill="none"
  stroke={color}
  stroke-width="16"
  stroke-linecap="round"
  stroke-linejoin="round"
  class={`icon icon-prototype ${className}`}
>
  <!-- Prototype Symbol (Wireframe box with arrows) -->
  <rect x="64" y="64" width="128" height="128" rx="12"></rect>
  <path d="M64 128h-24m176 0h-24M128 64v-24m0 176v-24"></path>
</svg>

<style>
  .icon-prototype {
    transition:
      transform 0.3s ease,
      stroke 0.3s ease,
      filter 0.3s ease;
    cursor: pointer;
    border-radius: 0.25rem; /* match previous rounded class */
  }

  .icon-prototype:hover {
    transform: scale(1.12);
    stroke: url(#gold-gradient);
    filter: drop-shadow(0 0 6px rgba(196, 176, 115, 0.4));
  }

  html.dark .icon-prototype:hover {
    filter: drop-shadow(0 0 8px rgba(233, 217, 153, 0.5));
  }
</style>

<!-- Gradient reusable -->
<svg width="0" height="0">
  <defs>
    <linearGradient id="gold-gradient" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#c4b073"></stop>
      <stop offset="100%" stop-color="#d9c98b"></stop>
    </linearGradient>
  </defs>
</svg>
