---
const {
  size = 24,
  color = "#0f7095", // 🎨 Default warna utama KVNS
  class: className = "",
} = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  width={size}
  height={size}
  viewBox="0 0 24 24"
  fill="none"
  stroke={color}
  stroke-width="2.2"
  stroke-linecap="round"
  stroke-linejoin="round"
  role="img"
  aria-label="Book icon"
  class={`transition-all duration-300 hover:scale-110 hover:drop-shadow-[0_0_6px_rgba(15,112,149,0.5)] icon-book ${className}`}
>
  <!-- ✨ Book outline -->
  <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
  <path d="M20 22H6.5A2.5 2.5 0 0 1 4 19.5V5a2 2 0 0 1 2-2h14z"></path>

  <!-- 🌈 Optional subtle gradient overlay -->
  <defs>
    <linearGradient id="bookGradient" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#0f7095"></stop>
      <stop offset="100%" stop-color="#4db3d3"></stop>
    </linearGradient>
  </defs>
</svg>

<style>
  /* ✨ Gradient stroke mode if user adds .gradient class */
  .icon-book.gradient path {
    stroke: url(#bookGradient);
  }
</style>
