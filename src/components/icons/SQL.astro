---
const {
  size = 26,
  color = "currentColor",
  class: className = "",
} = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  width={size}
  height={size}
  viewBox="0 0 24 24"
  fill="none"
  stroke={color}
  stroke-width="1.9"
  stroke-linecap="round"
  stroke-linejoin="round"
  class={`kvns-icon-sql transition-all duration-300 ease-out ${className}`}
>
  <!-- top disk -->
  <ellipse cx="12" cy="5.5" rx="7" ry="2.8"></ellipse>

  <!-- middle segment -->
  <path d="M5 5.5v6c0 1.6 3.2 2.8 7 2.8s7-1.2 7-2.8v-6"></path>

  <!-- bottom segment -->
  <path d="M5 11.5v6c0 1.6 3.2 2.8 7 2.8s7-1.2 7-2.8v-6"></path>

  <defs>
    <linearGradient id="kvnsSQLGF" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#c9b676"></stop>
      <stop offset="100%" stop-color="#e9dda5"></stop>
    </linearGradient>
  </defs>
</svg>

<style>
  .kvns-icon-sql {
    cursor: pointer;
  }
  .kvns-icon-sql:hover {
    transform: translateY(-2px) scale(1.11);
    stroke: url(#kvnsSQLGF);
    filter: drop-shadow(0 0 7px rgba(201, 182, 118, 0.42));
  }
  html.dark .kvns-icon-sql:hover {
    filter: drop-shadow(0 0 11px rgba(233, 217, 153, 0.55));
  }
</style>
