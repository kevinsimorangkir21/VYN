---
const {
  size = 26,
  color = "currentColor",
  class: className = "",
} = Astro.props;
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 24 24"
  width={size}
  height={size}
  fill="none"
  stroke={color}
  stroke-width="1.9"
  stroke-linecap="round"
  stroke-linejoin="round"
  class={`kvns-icon-mail transition-all duration-300 ease-out ${className}`}
>
  <rect x="3" y="5" width="18" height="14" rx="2"></rect>
  <path d="M3 7l9 6l9-6"></path>

  <defs>
    <linearGradient id="kvnsGoldMail" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#c9b676"></stop>
      <stop offset="100%" stop-color="#e9dda5"></stop>
    </linearGradient>
  </defs>
</svg>

<style>
  .kvns-icon-mail {
    cursor: pointer;
  }
  .kvns-icon-mail:hover {
    transform: translateY(-2px) scale(1.11);
    stroke: url(#kvnsGoldMail);
    filter: drop-shadow(0 0 7px rgba(201, 182, 118, 0.42));
  }
  html.dark .kvns-icon-mail:hover {
    filter: drop-shadow(0 0 11px rgba(233, 217, 153, 0.55));
  }
</style>
