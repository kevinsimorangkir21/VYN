<a
  {...Astro.props}
  target="_blank"
  rel="noopener noreferrer"
  role="link"
  class="inline-flex items-center justify-center gap-2 px-6 py-2.5
         rounded-full border text-base font-medium tracking-wide
         text-[#0f7095] border-[#0f7095]/40 bg-[#0f7095]/5
         dark:text-[#7fd0ea] dark:border-[#7fd0ea]/30 dark:bg-[#0f7095]/10
         backdrop-blur-sm transition-all duration-300 ease-out
         hover:scale-[1.04] hover:text-white hover:bg-[#0f7095]
         hover:shadow-[0_8px_20px_-5px_rgba(15,112,149,0.45)]
         focus:outline-none focus-visible:ring-2 focus-visible:ring-[#0f7095]/70 focus-visible:ring-offset-2
         active:scale-95 select-none group"
>
  <slot />
</a>

<style is:global>
  /* 🌟 Hover glow halus */
  a[role="link"]:hover {
    box-shadow: 0 6px 20px rgba(15, 112, 149, 0.25);
  }

  /* 🌓 Dark mode lebih lembut dan elegan */
  html.dark a[role="link"]:hover {
    box-shadow: 0 6px 18px rgba(127, 208, 234, 0.25);
  }

  /* 💫 Transisi menyeluruh */
  a[role="link"] {
    transition-property: color, background-color, border-color, transform,
      box-shadow;
  }

  /* ✨ Responsif kecil */
  @media (max-width: 400px) {
    a[role="link"] {
      font-size: 0.9rem;
      padding: 0.5rem 1.25rem;
    }
  }
</style>
